At the end of this project we are to understand how to make use of the following command lines:
$git clone <repo>
$git touch test
$ git add test
$ git commit -m "initial commit"
$git push origin main
